#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	go build -ldflags="-X main.version=$(shell dpkg-parsechangelog -SVersion)" -o u .

override_dh_auto_install:
	install -D -m 0755 u debian/u-cli/usr/bin/u
